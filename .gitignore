# Built artifacts
build/
app/build/
.gradle/

# Local configuration (machine-specific SDK paths)
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
*.iws
*.ipr
.DS_Store

# Signing keystore — keep release.jks out of version control
release.jks

# Firebase config — contains API keys, do not commit
app/google-services.json

# OS
Thumbs.db
